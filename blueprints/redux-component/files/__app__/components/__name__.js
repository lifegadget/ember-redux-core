import Ember from 'ember';
import ReduxComponent from '<%= dasherizedPackageName %>/components/redux-component';
import layout from '../templates/components/<%= dasherizedModuleName %>';

export default ReduxComponent.extend({
  layout
});
