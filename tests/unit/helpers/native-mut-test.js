import { nativeMut } from 'dummy/helpers/native-mut';
import { module, test } from 'qunit';

module('Unit | Helper | native mut');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nativeMut([42]);
  assert.ok(result);
});
