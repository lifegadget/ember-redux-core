import { foobar } from 'dummy/helpers/foobar';
import { module, test } from 'qunit';

module('Unit | Helper | foobar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = foobar([42]);
  assert.ok(result);
});
