
import { dispatch } from 'dummy/helpers/dispatch';
import { module, test } from 'qunit';

module('Unit | Helper | dispatch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dispatch([42]);
  assert.ok(result);
});

