import { redux } from 'dummy/helpers/redux';
import { module, test } from 'qunit';

module('Unit | Helper | redux');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = redux([42]);
  assert.ok(result);
});
