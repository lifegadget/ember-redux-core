import watch from 'dummy/utils/watch';
import { module, test } from 'qunit';

module('Unit | Utility | watch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = watch();
  assert.ok(result);
});
